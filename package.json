{
  "private": true,
  "scripts": {
    "format": "prettier --check \"**/*.{md,yml,yaml,json}\"",
    "format:fix": "prettier --write \"**/*.{md,yml,yaml,json}\""
  },
  "devDependencies": {
    "prettier": "^3.9.6"
  }
}
