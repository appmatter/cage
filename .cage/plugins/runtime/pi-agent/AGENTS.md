# Cage pi-agent (guest)

Synced from `.cage/plugins/runtime/pi-agent/` into the guest `~/.pi/agent/AGENTS.md`.
Does not replace a host Mac `~/.pi/agent/AGENTS.md`.

Default behavior is Cursor-like: map → reuse → implement → verify → evidence.
No special user prompt or `/skill` invocation is required. See `APPEND_SYSTEM.md`.
