# Dogfood profile: readable .cage (ro) for developing Cage itself.
extends: cage.yaml

fs:
  mount:
    .cage:
      host: .cage
      permission: ro
  deny:
    - path: .cage
      remove: true
    - path: .cage/cage.yaml
      remove: true
    - path: .cage/cage.*.yaml
      remove: true
